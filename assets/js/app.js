document.addEventListener('DOMContentLoaded', function(){ try { const cfg = window.APP_CHART || { labels:[], data:[] }; const ctx = document.getElementById('dailyChart').getContext('2d'); new Chart(ctx, { type:'line', data:{ labels: cfg.labels, datasets:[{ label:'Gain (₦)', data: cfg.data, fill:true, backgroundColor:'rgba(25,135,84,0.08)', borderColor:'#198754', tension:0.3 }]}, options:{ responsive:true, plugins:{ legend:{display:false}}, scales:{ y:{ ticks:{ callback: v=> { try{ return new Intl.NumberFormat('en-NG',{style:'currency',currency:'NGN'}).format(v);}catch(e){return '₦'+v;} } } } } }); } catch(e){ console.error(e); } });